# Keeps the historical HTTP port-5000 link working while sending users to
# the HTTPS deployment. Change server_name and the redirect target as needed.
server {
    listen 5000;
    listen [::]:5000;
    server_name pluto.cs.hi.is;

    return 301 https://pluto.cs.hi.is/voting/;
}
